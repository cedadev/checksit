var-requires:
  func: checksit.generic.check_var_attrs
  params:
    defined_attrs: 
      - long_name

required-global-attrs:
  func: checksit.generic.check_dim_exists
  params:
    dimensions:
      - time
       

