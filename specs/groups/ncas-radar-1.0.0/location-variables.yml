var-requires0:
  func: checksit.generic.check_var
  params:
    variable:
      - latitude
    defined_attrs:
      - type:double
    rules_attrs:
      - dimension: rule-func:match-one-of:--none--|time
var-requires1:
  func: checksit.generic.check_var
  params:
    variable:
      - longitude
    defined_attrs:
      - type:double
    rules_attrs:
      - dimension: rule-func:match-one-of:--none--|time
var-requires2:
  func: checksit.generic.check_var
  params:
    variable:
      - altitude
    defined_attrs:
      - type:double
    rules_attrs:
      - dimension: rule-func:match-one-of:--none--|time
