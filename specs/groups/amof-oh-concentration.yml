var-requires0:
  func: checksit.generic.check_var
  params:
    variables:
      - number_concentration_of_hydroxyl_radical_in_air
      - number_concentration_of_hydroperoxyl_radical_in_air
      - sum_of_hydroxyl_and_hydroperoxyl_radical_concentration_in_air
      - hydroxyl_radical_reactivity_in_air
    defined_attrs:
      - type
      - dimension
      - units
      - long_name
      - _FillValue
      - valid_min
      - valid_max
      - coordinates
var-requires1:
  func: checksit.generic.check_var
  params:
    variables:
      - qc_flag
    defined_attrs:
      - type
      - dimension
      - units
      - long_name
      - flag_values
      - flag_meanings

